name: workflow
description: "Smart Task Manager with Integrated Pomodoro"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  # REMOVED: flutter_local_notifications
  # REMOVED: font_awesome_flutter
  # REMOVED: flutter_spinkit

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true